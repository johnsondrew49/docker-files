services:
  termix:
    image: ghcr.io/lukegus/termix:latest
    container_name: termix
    restart: unless-stopped
    ports:
      - "8050:8050"
    volumes:
      - termix-data:/app/data
    environment:
      PORT: "8050"

volumes:
  termix-data:
    driver: local
